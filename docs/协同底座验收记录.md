# 协同底座验收记录

日期：2026-09-11。全部验证在本地完成，未连接腾讯生产服务器、未消耗第三方模型额度。

| 检查 | 结果 | 范围 |
|---|---|---|
| Python离线/协议测试 | 29项通过 | 原数据契约16项；API/节点/模型13项 |
| PostgreSQL17真实容器 | 通过 | 10个任务并发领取，ID唯一，10份结果事务提交；数据库临时且独立 |
| Docker API镜像 | 构建成功 | Python3.12-slim，本机arm64；未验证目标服务器架构构建 |
| 容器真实HTTP | 通过 | health、401鉴权、创建/领取/回传/读取；此项使用临时SQLite |
| 模型协议 | 测试桩通过 | JSON、证据ID、缓存、usage和超时不重试；未调用真实模型 |
| 浏览器采集 | 本轮未重跑 | 沿用前一轮人工实页观察；Python真实DOM配方仍未验收 |
| 生产部署 | 未执行 | 域名/端口/目录/凭据尚未指定 |

第一次PostgreSQL容器初始化报No space left on device，未清理任何其他项目资源；测试改为256MiB临时内存数据盘后通过，因此不代表Docker持久磁盘空间问题已解决，也不验证生产持久卷恢复。数据库重启恢复测试目前通过SQLite文件重开验证，PostgreSQL持久卷重启需另测。

执行方式：

```bash
.venv/bin/python -m unittest discover -s tests -v
PYTHONPATH=.:tests .venv/bin/python tests/verify_postgres.py
docker build -f deploy/cloud/Dockerfile -t appcatch-local-verification:latest .
```

verify_postgres.py只创建/删除自己命名的临时测试容器，不接收生产数据库地址。模型及采集器测试使用标明TEST ONLY的数据；不会写入实际业务库。

当前限制：固定单管理员工作空间、固定单Mac节点；没有Web登录、账号权限系统、节点凭据轮换页面、业务配额、模型预算账本、自动分析队列。模型适配器与云端任务API尚未自动串联。下一阶段按v2架构B→C→D推进。
