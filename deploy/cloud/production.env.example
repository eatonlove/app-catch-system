# Configure locally; never commit real values. URL-encode DB password in the URL.
AC_DB_PASSWORD=
AC_DATABASE_URL=postgresql+psycopg://appcatch:REPLACE@db:5432/appcatch
AC_ADMIN_TOKEN=
AC_WORKER_TOKEN=
AC_HOST_PORT=
AC_ADMIN_PASSWORD=
AC_RELEASE=
# Optional: called only for explicitly requested model research.
AC_LLM_ENDPOINT=https://dashscope.aliyuncs.com/compatible-mode/v1
AC_LLM_MODEL=deepseek-v4-flash-0731
AC_LLM_API_KEY=
AC_MODEL_DAILY_LIMIT=10
